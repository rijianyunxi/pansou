# 紧急修复清单

> 目标：配置迁移到 SQLite 后，清理旧配置残留和无用旧代码，并保证迁移数据不丢失。
> 审计日期：2026-09-14

## 1. P0：修复 SQLite 迁移遗漏

- `server/core/storage/sqlite.ts` 当前只迁移 `legacy_kv`，但旧版本使用过 `json_store`。
- 当前运行数据库仍存在 `json_store` 及旧记录；不能直接删除旧表。
- 当前 `migrateLegacyValue()` 没有完整处理：
  - `search_settings`
  - `system_settings`
  - `tg_source_settings`
- 已复现：临时数据库中旧记录未写入对应结构化表，却被标记为 `normalized_storage_v1` 已完成。
- 要求：兼容 `json_store` 和 `legacy_kv`，补齐上述命名空间迁移，迁移必须幂等且只在目标数据成功落库后标记完成。

## 2. P1：统一正式运行时的配置来源

- `server/core/services/upstreamCatalog.ts` 仍通过 SQLite 的旧 KV 兼容接口 `get/set/getUpdatedAt` 读写上游目录。
- `server/core/services/tgChannelSettings.ts` 写入仍调用旧 KV 兼容接口。
- `server/utils/searchDefaults.ts` 在数据库设置为空时直接回退 `useRuntimeConfig()` 的默认频道。
- `server/core/services/index.ts` 的 `cacheTtlMinutes` 仍直接使用 runtime config，而不是 `getSystemSettings()` 的 SQLite 值。
- 要求：正式业务读取和写入优先使用结构化 SQLite 表；默认配置只作为首次初始化种子，不应覆盖已保存的 SQLite 设置。

## 3. P1：清理旧 JSON 兼容层及其失效测试

- `JsonPluginRepository`、`JsonPluginSecretStore`、`JsonPluginHealthStore` 已不再写 JSON 文件，其中部分仅为空兼容别名。
- `SqliteJsonStoreOptions`、若干 `NAMESPACE/KEY` 常量、`emptyState`、未使用 sanitize 函数等已被未使用检查确认。
- `test/unit/configConsistency.test.ts` 仍测试已废弃的 JSON 文件和 `PANHUB_*_STORE` 环境变量。
- `test/unit/secretStore.test.ts` 仍直接断言 `plugin-secrets.json`。
- 要求：将测试改为 SQLite 隔离/持久化语义，删除确认无调用方的旧文件兼容测试和无用声明；不得破坏仍需保留的迁移测试。

## 4. P2：Telegram Session 路径

- MTProto 当前仍使用 `TELEGRAM_SESSION_FILE` 读写 `data/telegram-session.txt`。
- TG 账户表虽有 `session_string`，但 MTProto 登录流程尚未接入账户仓库。
- 本项先保留文件登录流程，避免误删登录能力；后续单独设计迁移到账户表。

## 5. 已知验证基线

- 相关配置测试此前为 93 个用例，84 通过、9 失败；失败主要来自旧 JSON 文件断言以及兼容类共享 SQLite 的隔离问题。
- 未使用检查确认的代表性问题：
  - `server/core/parsers/repository.ts` 的 `NAMESPACE/KEY`
  - `server/core/plugins/repository.ts` 的 `NAMESPACE/KEY`
  - `server/core/services/systemSettingsService.ts` 的 `NAMESPACE/KEY`
  - `server/core/services/tgSourceSettings.ts` 的 `NAMESPACE/KEY`
  - `server/core/telegram/accountStore.ts` 的 `NAMESPACE/KEY`
  - `server/core/services/tgChannelHealthStore.ts` 的 `DB_NAMESPACE/DB_KEY`
  - `server/core/services/searchSettingsService.ts` 的 `DEFAULT_SETTINGS`
  - `server/core/services/tgChannelSettings.ts` 的 `emptyState/sanitizeParsers/sanitizeUpstreamParsers`
  - `components/monitor/MonitorPanel.vue` 的 `resetFilters`

## 6. 修复顺序

1. 先修迁移兼容和幂等性，确保历史数据不丢失。
2. 再统一正式运行时配置来源。
3. 再清理旧兼容代码和过时测试。
4. 跑 `pnpm test`、`pnpm typecheck`，按失败结果继续修复。
5. 检查 diff、提交并推送到当前分支 `main`。

## 7. 本轮修复结果

- 已兼容 `json_store` 与 `legacy_kv`，补齐三类遗漏配置迁移，并保证迁移幂等及事务内最后写入完成标记。
- 已将上游目录、TG 频道设置、搜索默认频道和缓存 TTL 改为读取结构化 SQLite / 系统设置。
- 已删除无调用方的 JSON 仓库别名、无用常量和函数，并将相关测试改为 SQLite 隔离和持久化断言。
- 已删除测试中失效的 `PANHUB_*_STORE` 配置残留，以及无调用方的 `sessionFileExists`、`SqliteJsonStoreOptions`。
- 保留 `TELEGRAM_SESSION_FILE`：当前 MTProto 登录流程仍依赖该会话文件，待后续专门迁移到 `tg_accounts.session_string`。

## 8. 验证结果

- `pnpm test`：59 个测试文件通过，554 个用例通过。
- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- `git diff --check`：通过。
